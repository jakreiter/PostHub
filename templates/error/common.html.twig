{% extends 'base.html.twig' %}

{% block body %}


    <div class="alert alert-warning alert-dismissible niezaszer" role="alert">
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
     	<div class="container-fluid">
        	<div class="row">
        		<div class="col-md-2">
        			<span class="duzaikona"><span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span></span>
        		</div>
        		<div class="col-md-10">
        			     	<h1>{{ error.title }}</h1>
        					<p>{{ error.description }}</p>
        		</div>
			</div>
		</div>
     	

    </div>


{%  if comment is defined  %}{{ comment }}{% endif %}
{% endblock %}
