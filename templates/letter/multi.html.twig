{% extends 'base.html.twig' %}

{% block title %}{{ "Adding information about multiple letters"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Adding information about multiple letters"|trans }}</h1>

    <div id="forforms" data-controller="forforms" data-form-number="0"></div>

    <div id="template_form">
        {% set form=form0 %}
        {{ include('letter/_form.html.twig') }}
    </div>

    <br>
    <button class="oneMoreForm btn btn-primary">{{ "One more form"|trans }}</button>
    <button class="fiveMoreForms btn btn-primary">{{ "5 more forms"|trans }}</button>
{% endblock %}
