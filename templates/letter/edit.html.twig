{% extends 'base.html.twig' %}

{% block title %}{{ "Edit Letter"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Edit Letter"|trans }}</h1>

    {{ include('letter/edit_form.html.twig', {'button_label': 'Save'|trans}) }}


    <div class="container-fluid">
        <div class="row">
            <div class="col-md-2">
                <a href="{{ path('letter_admin_index') }}" class="btn btn-secondary btn-block">{{ "Back to list"|trans }}</a>
            </div>
            <div class="col-md-2">
                <a href="{{ path('dh_auditor_show_entity_history', {'id': letter.id, 'entity': 'App-Entity-Letter'}) }}"
                   class="btn btn-outline-info btn-block">{{ "Audit"|trans }}</a>
            </div>
            <div class="col-md-2">
                {{ include('letter/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
