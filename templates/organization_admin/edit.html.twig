{% extends 'base.html.twig' %}

{% block title %}{{ "Edit Organization"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Edit Organization"|trans }}</h1>

    {{ include('organization_admin/_form.html.twig', {'button_label': 'Save'|trans}) }}


    <div class="container-fluid">
        <div class="row">
            <div class="col-md-2">
                <a href="{{ path('organization_admin_index') }}" class="btn btn-secondary btn-block">{{ "Back to list"|trans }}</a>
            </div>
            <div class="col-md-2">
                <a href="{{ path('dh_auditor_show_entity_history', {'id': organization.id, 'entity': 'App-Entity-Organization'}) }}"
                   class="btn btn-outline-info btn-block">{{ "Audit"|trans }}</a>
            </div>
            <div class="col-md-2">
                {{ include('organization_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
